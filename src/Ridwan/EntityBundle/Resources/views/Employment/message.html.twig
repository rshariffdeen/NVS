{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Employment list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Occupation</th>
                <th>Organizationtype</th>
                <th>Organization</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('employment_show', { 'id': entity.id }) }}">{{ entity.occupation }}</a></td>
                <td>{{ entity.organizationtype }}</td>
                <td>{{ entity.organization }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('employment_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('employment_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('employment_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
