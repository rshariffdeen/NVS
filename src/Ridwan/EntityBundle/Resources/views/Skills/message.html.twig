{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Skills list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Skills</th>
                <th>Causes</th>
                <th>Other</th>
                <th>Languages</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('skills_show', { 'id': entity.id }) }}">{{ entity.skills }}</a></td>
                <td>{{ entity.causes }}</td>
                <td>{{ entity.other }}</td>
                <td>{{ entity.languages }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('skills_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('skills_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('skills_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
